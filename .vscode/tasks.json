{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Ensure Power Query SDK Tools",
      "type": "process",
      "command": "powershell.exe",
      "args": [
        "-NoProfile",
        "-ExecutionPolicy",
        "Bypass",
        "-Command",
        "& { $toolsPath = Join-Path \"${workspaceFolder}\" \".pqtools\\Microsoft.PowerQuery.SdkTools.2.152.3\\tools\"; if (Test-Path (Join-Path $toolsPath 'PQTest.exe')) { Write-Host 'Power Query SDK tools already present.'; exit 0 }; $root = Join-Path \"${workspaceFolder}\" '.pqtools'; $version = '2.152.3'; $pkgName = \"Microsoft.PowerQuery.SdkTools.$version\"; $pkgDir = Join-Path $root $pkgName; $nupkg = Join-Path $root \"$pkgName.nupkg\"; $zip = Join-Path $root \"$pkgName.zip\"; New-Item -ItemType Directory -Path $root -Force | Out-Null; Invoke-WebRequest -Uri \"https://api.nuget.org/v3-flatcontainer/microsoft.powerquery.sdktools/$version/microsoft.powerquery.sdktools.$version.nupkg\" -OutFile $nupkg; Copy-Item $nupkg $zip -Force; if (Test-Path $pkgDir) { Remove-Item $pkgDir -Recurse -Force }; Expand-Archive -Path $zip -DestinationPath $pkgDir -Force; if (-not (Test-Path (Join-Path $toolsPath 'PQTest.exe'))) { throw 'Power Query SDK tools installation failed.' }; Write-Host \"Installed Power Query SDK tools at $toolsPath\" }"
      ],
      "problemMatcher": []
    },
    {
      "label": "Build Opinyin Connector (MEZ)",
      "type": "process",
      "command": "C:\\Program Files\\dotnet\\dotnet.exe",
      "args": [
        "msbuild",
        "Opinyin Classifications Power Query Connector.proj",
        "/t:BuildMez",
        "/p:Configuration=Debug",
        "/v:m"
      ],
      "dependsOn": [
        "Ensure Power Query SDK Tools"
      ],
      "dependsOrder": "sequence",
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": []
    },
    {
      "label": "Clean Opinyin Connector",
      "type": "process",
      "command": "C:\\Program Files\\dotnet\\dotnet.exe",
      "args": [
        "msbuild",
        "Opinyin Classifications Power Query Connector.proj",
        "/t:Clean",
        "/p:Configuration=Debug",
        "/v:m"
      ],
      "problemMatcher": []
    }
  ]
}
